/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true, // App Router を有効にする設定
  },
};

module.exports = nextConfig;
module.exports = {
  experimental: {
    appDir: true,
  },
};
